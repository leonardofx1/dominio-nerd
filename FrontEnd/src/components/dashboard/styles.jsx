import styled from 'styled-components'
import { BrowserRouter } from 'react-router-dom'

export const Main = styled(BrowserRouter)`
    background: var(--cor-light);
    display: flex;
    min-height: calc(100vh - 100px);
`

